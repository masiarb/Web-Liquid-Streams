/**
 * New node file
 */
var k = require('./../k_globals/koala.js')


k.createNode(function(message,uid,options) {

	// console.log(options)
	// console.log(message)
	// var data = JSON.parse(JSON.parse(message.data))
	// if(message.stream) {
	// 	var personalId = message.pid
	// 	var data = JSON.parse(message.stream).data

	// 	allImages[personalId] = data

	// 	if(setDom) {
	// 		setDom('#images', 'html', JSON.stringify(allImages), {})
	// 	}
	// }

	k.send({},options)

})

// k.createHTML('esempio_webcam_html_consumer' ,
// 	'<div style="visibility: hidden" id="images"></div>' +
//     '<div id="list"></div>'
// )

// k.createScript('esempio_webcam_consumer', 'js/webcam_consumer.js')

console.log('webcam consumer started') 
 