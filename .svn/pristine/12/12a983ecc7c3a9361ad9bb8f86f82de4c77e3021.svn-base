/**
 * ECHO FILTER
 * Takes whatever input it receives and forwards it.
 * It sends the exact data it received.
 */

var k = require('./../k_globals/koala.js');
  
k.createNode(function(msg, uid) {


	k.send(msg.data);
	
	
}).start();